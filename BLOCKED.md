# 待裁决清单

## 本次客户用户手册任务

- 已解决：任务 0 的 HTTP 检查返回 `200`，但沙箱内首次运行截图命令后，`sips` 报 `/tmp/t0.png not a valid file`、`stat` 报文件不存在；改用同一截图命令的受控执行后已得到 `1680` 宽、`245628` 字节的线上页面截图。
- 无新增阻塞。

## 历史待裁决

- `frontend/src/pages/Dashboard.tsx` 原有 KPI 卡仍包含硬编码涨跌文案（例如 `↑ 18%`、`↓ 3pt`）。任务明确要求不顺手改动，本次保留，待后续另行裁决其指标口径与 i18n 化方案。

## 本次用户手册流程脚本化

- 无新增阻塞。首次 `--check-only` 因尚无 manifest 正确失败；生成 manifest 后 `--check-only` 通过。
