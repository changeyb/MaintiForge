# 执行进度

- 目标：为维修车间 demo 增加 `/poc` 数据血缘、PoC 验收、ROI 展示与收尾提案，帮助客户相信数据来源并购买 PoC。
- 顺序：先追加 ROI 推导，再完成页面/i18n/导航路由，随后补 Dashboard banner 与提案，最后做构建、红绿反向验证和截图验收。
- 边界：仅修改任务白名单文件；不碰 mock 数据、依赖、配置和已有 i18n 文案。
- 已核对：初始 build 通过；mock/data.ts 与 package.json 指纹符合要求；totalWaitLossWeek 存在。
- 最大风险：i18n 中英文 key 同步、页面数字血缘、深色大屏布局与无头截图验收。
- 进度：全部完成。最终 build 通过；mock/package 指纹不变；zh/en=248；反向删 key 红→恢复绿已验证；首页与 `/poc` 截图均超过 80KB；`BLOCKED.md` 已记录既有硬编码涨跌文案。

## 本次客户用户手册任务

- 目标：基于线上 `http://47.97.66.237/` 的真实页面截图，交付客户可独立翻阅的中文《用户手册》。
- 顺序：先核验线上 200 与首张截图，再读 `PoC_提案.md` 第 3、5 节，截齐 8 张图，写手册，最后做逐项验收。
- 内容口径：严格沿 5 步演示故事线，数字只取提案或线上页面实际显示；图片只用相对路径。
- 变更边界：只新增 `docs/` 下手册与截图，更新 `PROGRESS.md`、`BLOCKED.md`，不改 `frontend/`、配置或提交 git。
- 最大风险：线上截图可能未加载或尺寸不符；其次是故事线数字、路由和客户向措辞不一致。
- 任务 0：线上首页 HTTP 200；受控 Chrome 生成真实首页截图 1680 宽、245628 字节，已目视确认中文页面。
- 任务 1：已从线上生成 8 张 `docs/screenshots/*.png`，并完成错误端口坏图反向验证（32552 字节后删除）。
- 任务 2：已完成中文客户向手册，覆盖快速上手、5 步路线、其他页面速览和 6 条 FAQ；最终门禁与工作树检查通过。
- 最终验收：9 个图片引用均逐一 `test -f` 通过；8 个截图均为 1680 宽且大于 100KB；文字门禁为图片 9、线上地址 11、二级章节 4、车辆故事线 5。
- 补充尺寸验收：8 张截图逐一确认均为 `1680×1050`，且文件大小均大于 `100KB`；PoC 第 3、5 节口径与手册数字已复核一致。
- 最终线上复核：绕过已断开的本地 `127.0.0.1:1080` 代理直连 `http://47.97.66.237/` 返回 `200`；不影响已生成的线上截图。
- 交付状态：手册与截图已完成；工作树仅有 `docs/`、`PROGRESS.md`、`BLOCKED.md` 改动；未提交 git。

## 本次用户手册流程脚本化

- 目标：把手册模板、线上 8 路由截图、内容/图片门禁、坏路由反向验证和 SHA-256 manifest 收敛为可复用入口。
- 固定命令：`python3 scripts/generate_user_manual.py --config configs/user_manual.toml`；只读复核使用 `--check-only`。
- 产物：`scripts/templates/user_manual.md` 作为正文源，生成 `docs/用户手册.md`、`docs/screenshots/*.png` 和 `docs/user-manual-manifest.json`。
- 已验证：脚本真实访问线上站点并生成 8 张 `1680×1050` 截图；坏路由反向图 `36428` 字节后自动删除；manifest 记录配置与模板 SHA-256。
- 已验证：`--check-only` 通过且手册/manifest 哈希不变；8 张截图已人工目视复核；Python 语法与 `git diff --check` 通过。
- 设计取舍：保留人工视觉复核标记，自动门禁只证明尺寸、大小、引用、内容和哈希，不把脚本绿灯当成页面观感验收。
- 变更范围：新增 `scripts/`、`configs/`、manifest 和 README 使用说明；`frontend/` 未改动，未提交 git。

## 本次 README/AGENTS 同步

- 已在 `README.md` 补充仓库结构、固定生成入口、线上 URL 约束、配置/模板职责和 `--check-only` 用法。
- 已在 `AGENTS.md` 补充后续 agent 必须遵守的手册生成、截图门禁、manifest 哈希和人工视觉复核流程，并区分前端本地截图与客户手册线上截图。
- 验证：`git diff --check` 通过，两个文档均能定位到当前脚本和配置入口；`frontend/` 未改动。
